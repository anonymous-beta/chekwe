# Core
SECRET_KEY=change-me-to-a-long-random-string
DATABASE_URL=sqlite:///./chekwe.db
# Production: postgresql+psycopg://chekwe:chekwe@db:5432/chekwe
DEMO_MODE=true
CORS_ORIGINS=http://localhost:5173
# Never put AI provider keys here — they are stored encrypted in the DB
# via Settings → AI Provider. This file is for server infra only.
